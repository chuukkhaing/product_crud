{% extends 'base.html.twig' %}

{% block title %}Create Product{% endblock %}

{% block body %}
    <h1>Create new Product</h1>
    {{ include('product/_form.html.twig') }}
    <a href="{{ path('product_index') }}" class="btn btn-secondary btn-sm">Back to list</a>
{% endblock %}
